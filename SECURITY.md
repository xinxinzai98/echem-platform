# V0.2 数据工作台与 V0.3 阶段 B 安全边界

## 平台会写入的内容

平台只在自身 `state` 目录中写入 SQLite 数据库及其事务文件。保存的内容包括：

- 原始文件路径、大小、修改时间与 SHA-256
- 解析后的降采样曲线
- 样品编号、材料、电解液、面积、标签和备注
- 导入与人工编辑审计记录
- 网页协议草稿、输出目录文本和草稿修订号

离线编译器默认只在内存中生成宏。只有用户明确提供 `--macro-output` 时，才会在
指定的本机位置独占创建一个 `.mcr` 文件；已有文件不会被覆盖。

网页 Dry-run 始终只在内存中生成预览，不提供 `.mcr` 写出入口。草稿保存不要求
协议已经通过校验，便于保留尚未完成的编辑；校验或编译时仍会重新执行全部严格
检查。

## 平台不会执行的操作

- 不创建、重命名、覆盖或删除被监控目录中的文件
- 不打开 COM3、COM4 或其他串口
- 不启动、停止或控制 CHI760E / CS Studio6
- 不执行、投递或远程触发 CHI 宏
- 不加载未经确认的 CorrTest SDK
- 不监听局域网地址；程序会拒绝 `0.0.0.0` 等非回环绑定
- 不上传云端，不调用外部网络服务

## V0.3 阶段 A 离线编译边界

- 配置项 `instrument_control_enabled` 只能为 `false`；设为 `true` 会直接拒绝启动
- 控制模块不包含进程启动、串口或网络控制入口
- 宏文件固定使用 `43 02 00 00 0A` 文件头和纯 ASCII 正文
- 只允许已经确认的命令白名单，拒绝未知命令、`fileoverride` 和 `cellon`
- 输出目录必须是指定 Windows 运行根目录的严格子目录，并拒绝 `..`、空格、非
  ASCII 字符和 Windows 保留名称
- 每个工步必须形成完整的 `tech`、参数、`run`、`save`、`tsave` 序列，输出名
  不区分大小写且不能重复
- 协议中的样品名、材料、电解液、操作人和备注不会写入宏正文
- 编译后立即重新解析并静态复核整个二进制；复核失败时不返回可用宏

这些检查只证明文件结构满足当前白名单，不证明参数适合某个电极、体系或工作站
状态。生成的宏必须先进行人工参数复核和 Windows 端 dry-run；阶段 A 不授权真实
仪器执行。

## V0.3 阶段 B 网页 Dry-run 边界

- `/api/control/capabilities` 明确返回
  `instrument_control_enabled=false`、`instrument_started=false`、
  `launch_available=false`、`serial_access=false`
- 网页只提供草稿保存、读取、列表、校验和编译预览接口
- 服务端没有启动 CHI、提交宏、控制运行、访问串口或连接远程 Windows 的路由
- `/api/protocols/validate` 不返回宏正文；只有 `/api/protocols/compile` 返回当前
  请求在内存中生成的预览
- 网页请求体限制为 1 MiB，拒绝非 JSON、非对象、无效 UTF-8 和未知字段
- 静态页面使用同源 CSP，并禁止被其他页面嵌入
- 参数变化后旧校验和旧宏预览立即失效
- 输出目录越出允许根目录、输出文件名重复或协议字段不合法时，不显示宏预览
- EIS Points/Decade 和无法精确计算的 EIS 时长会转为明确的人工复核项

阶段 B 仍不是 Windows 宏导入测试，也不是仪器执行阶段。后续若增加任何进程启动
或硬件入口，必须使用新的阶段、独立确认和新的安全测试，不能复用当前 Dry-run
页面暗中启用。

## 正在写入的文件

当文件修改时间距当前不足配置中的 `stable_age_seconds`，平台会暂缓读取。读取前后若大小或修改时间发生变化，同样会放弃本次导入并等待下一轮扫描。

## 文件完整性

每条记录保存完整 SHA-256。相同路径、相同哈希和相同解析器版本不会重复导入；解析器升级时会重新解析，并保留平台中的样品信息。

平台会在读取列表和详情时动态检查源文件是否仍存在。源文件被移动或删除时，平台只显示“源文件不可用”，不会自动删除数据库中的缓存曲线、文件指纹或人工填写的样品信息。

## 公开仓库与私有数据

- 实际监控路径只写入被 Git 忽略的 `config.local.json`
- 真实实验文件、盘点结果和未脱敏测试样例不得提交到公开仓库
- 厂商帮助文件、SDK、安装包和其他受许可约束的文件不得提交
- 每条导入记录保存解析器标识和版本，解析行为升级后可重新复核

## 私有样例验证器

- `private_fixtures/` 整体被 Git 忽略
- 清单只能引用其所在目录内的相对路径；绝对路径、`..` 和指向目录外的符号链接会被拒绝
- 验证器读取前后复核文件大小、修改时间和 SHA-256，不写入样例
- JSON 报告只包含匿名 case id、扩展名、大小、SHA-256 和解析结果，不包含路径、文件名或原始内容
- case id 会出现在报告中，因此必须使用匿名编号
- 验证器默认只向标准输出打印结果，不自动创建报告文件

## 当前版本明确不承担的安全功能

平台不是仪器联锁或急停系统，不能替代工作站本身的电流、电压、时间限制，也不能替代本地人工巡视。离线编译成功不等于实验方案获批或仪器运行安全。

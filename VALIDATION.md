# V0.2 数据工作台与 V0.3 阶段 B 开发验证记录

验证日期：2026-07-24
数据工作台版本：0.2.0-dev.3

网页 Dry-run 版本：0.3.0-dev.2

CHI 离线协议编译器版本：0.3.0-dev.1

## 自动化测试

共 51 项，全部通过：

- 6 项解析器测试
  - CHI CV 文本识别与坐标轴解析
  - CorrTest EIS Nyquist 坐标轴解析
  - CorrTest GalStatic 默认使用电位—时间曲线
  - CHI `.bin` 仅登记元数据
  - 曲线降采样保留首尾点
  - CHI / CorrTest 文本解析逻辑已从主程序拆分为独立模块
- 5 项扫描与数据库测试
  - 扫描前后源文件内容、大小和修改时间不变
  - 样品信息只写平台数据库
  - 正在写入的文件暂缓导入
  - 源文件移走后只标记不可用，不删除缓存曲线或数据库记录
  - 未完成或未通过校验的协议草稿仍可保存，并按修订号恢复
- 4 项私有样例验证测试
  - CHI / CorrTest 清单断言通过且源文件读取前后不变
  - 拒绝使用 `..` 读取清单目录之外的文件
  - 断言失败时返回匿名结果，不泄露路径或文件名
  - 清单缺失或不可读取时，错误信息不泄露清单路径
- 4 项安全与配置测试
  - 拒绝绑定 `0.0.0.0` 等非回环地址
  - 相对监控路径以配置文件目录为基准
  - 被 Git 忽略的 `config.local.json` 能覆盖本机路径
  - 本地覆盖文件不能绕过回环地址限制
- 6 项协议模型测试
  - CV 圈数只在初始端点和方向明确时转换为扫描段数
  - 拒绝含糊的 CV 圈数换算、重复输出名和全部禁用的协议
  - 拒绝尚未确认的 EIS `points_per_decade` 字段
  - 用户元数据可保留在规范化快照中，但不会要求转为 ASCII
- 13 项宏编译与静态复核测试
  - 固定 5 字节文件头、宏体 SHA-256 与长度快照
  - 相同协议生成完全一致的二进制和协议指纹
  - 用户元数据不进入宏，且不会生成 `fileoverride` 或 `cellon`
  - 拒绝错误文件头、ASCII 控制字符、未知或禁止命令、越界参数
  - 拒绝缺失的 `save` / `tsave` 配对和 EIS `impft`
  - 拒绝运行根目录逃逸、空格、非 ASCII、`..` 和 Windows 保留名称
- 8 项离线控制与网页 Dry-run 安全测试
  - 配置不能启用仪器控制，三个公开配置均明确为 `false`
  - 控制包不包含软件启动、串口或网络控制入口
  - 命令行工具只创建新 `.mcr`，拒绝覆盖已有文件
  - 网页能力声明没有启动、串口或仪器控制能力
  - 校验响应不包含宏正文，编译预览只在内存中生成
  - Dry-run 请求拒绝未知字段且不会写出文件
- 5 项网页 API 与页面测试
  - 草稿保存、列表、读取和修订恢复
  - 校验与编译接口不写文件、不启动进程
  - 校验错误返回稳定的字段路径和错误码
  - 页面使用限制性响应头，且没有启动路由

Python 模块编译与前端 JavaScript 语法检查均通过。

## V0.3 阶段 A 离线编译验收

- 公开示例协议可在 Mac 上完成 JSON 校验、规范化、内存编译和静态复核
- 二进制以 `43 02 00 00 0A` 开头，正文为 ASCII 且以 LF 换行
- 编译报告明确返回 `instrument_started=false` 和
  `instrument_control_enabled=false`
- `--macro-output` 使用独占创建；第二次写入同一路径时拒绝覆盖且原文件不变
- 控制配置设为 `true` 时立即失败
- 本阶段未连接 Windows、未启动 CHI、未执行宏、未访问串口或仪器

## V0.3 阶段 B 网页 Dry-run 验收

- 默认单工步草稿可生成内存预览，显示 20 s 预计时长、BIN/TXT 目标文件、固定
  文件头、协议摘要和宏摘要
- CV、OCP、LSV、EIS 四种工步均可添加；复制、排序、删除、启用和禁用操作正常
- 5 个工步、其中 4 个启用的草稿可保存并在刷新后完整恢复名称、顺序、保存名和
  启用状态
- 含 EIS 时显示 `40 s + 未知工步`，并明确提示 Points/Decade 与时长需要人工复核
- 重复输出文件名会返回字段级错误，旧宏预览立即隐藏
- 输出目录越出允许根目录会返回 `run_root_escape`，不生成可见宏
- 正常页面刷新后浏览器控制台错误和警告均为 0
- 页面请求仅包含能力查询、草稿读写、校验和编译预览，没有启动或运行接口
- 原有数据工作台可正常打开，控制台错误和警告均为 0，并可返回协议页面
- 本轮真实浏览器验收只连接 Mac 本机回环服务；未连接 Windows 或仪器

## 私有样例验证器冒烟测试

- 使用 1 个 CHI CV 和 1 个 CorrTest EIS 公开演示文件模拟私有目录
- 2 个 case 全部通过
- `read_only=true`
- 两个 case 均为 `source_unchanged=true`
- 输出包含解析器版本和完整 SHA-256
- 输出不包含绝对路径、文件名或原始实验内容

## 模拟数据扫描

- 首次扫描：发现 4，导入 4，错误 0
- 重复扫描：发现 4，unchanged 4，错误 0
- 可绘制曲线：3
- 二进制元数据记录：1
- 每条记录均保存解析器标识与解析器版本

解析器标识覆盖：

- `chi.delimited_text`
- `chi.binary.metadata`
- `corrtest.z60_text`
- `corrtest.delimited_text`

## 本地浏览器验收

- 状态卡显示 4 条记录、3 条可绘制曲线
- CHI / CorrTest 仪器筛选正常
- CHI 筛选后只显示 2 条 CHI 记录
- 曲线详情显示解析器标识
- 状态卡、记录列表和详情均显示源文件可用性
- 源文件不可用时，缓存曲线和人工填写的样品信息仍可查看
- 旧记录缺少解析器标识时不会显示空白标签
- 审计记录显示解析器标识
- 浏览器控制台错误：0
- 服务只监听 `127.0.0.1:8787`

## Windows 只读盘点

- 只读取路径、扩展名、数量、大小和修改时间
- 未读取或复制实验文件内容
- 未访问 COM3 / COM4
- 未调用 CHI 宏或 CorrTest SDK
- 当前没有稳定的真实 CHI 文本导出、CorrTest `.cor` 或 `.z60` 样例
- 精确路径和实验名称只保存在被 Git 忽略的本机盘点记录中

## 尚未执行

- V0.2 尚未部署到 Windows
- 尚未复制或解析真实实验样例
- 尚未进行 Windows 持续扫描测试
- 尚未访问 COM3 / COM4
- 尚未在本平台执行或远程触发 CHI 宏
- 尚未安装或调用 CorrTest SDK
- 尚未进行 Windows 端宏导入 dry-run
- 尚未进行任何真实仪器闭环测试

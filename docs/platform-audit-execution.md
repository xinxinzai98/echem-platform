# 审计整改执行记录

目标：完整执行 `output/platform-audit-20260907/审计报告.md` 的 A01–A15 及七个桌面视图验收要求。
基线：0.6.0-dev.7.10；2026-09-07 全量 518 项测试通过。现有未提交修改保留。

| 项目 | 实现状态 | 完成所需证据 |
|---|---|---|
| A01 全量统计与显示抽样分离 | 已实现；真实 601 条与大样例、截断检查通过；待部署 | >200,000 点、14.9/15/15.1 s、末端窗口；不同预览点数统计不变；旧抽样源不输出正式统计 |
| A02 CV/EIS 配对与人工确认 | 代码、数据库、HTTP 与浏览器确认流程通过；待部署 | 禁止前后测互配；模糊候选/旧 EIS/缺时间测试；人工选择带修订与来源 |
| A03 图表最后选择生效 | JS 乱序与浏览器慢网/断网恢复通过；待部署 | 实际 JS 慢响应 A→B→C；清空/切指标/失败恢复；图例和统计一致 |
| A04 在线补偿三态 | 三态、持久化与浏览器流程通过；待部署 | true/false/unknown；未知原始预览和确认流程；不得自动假定未补偿 |
| A05 本机/LAN 统一状态 | 已实现共享读取与过期状态投影；待线上对照 | 版本/时间/修订/备份/封存/收藏一致；脱敏边界测试 |
| A06 分析和 PDF 状态解耦 | 无 PDF 就绪及独立网页生成/下载通过；待部署 | 无 PDF 仍分析就绪；新数据/配置过期分别提示 |
| A07 默认 16 条 | 实际函数和浏览器默认 16 条通过；待部署 | 首次≤16；收藏/近期优先；允许扩大选择；切工步一致 |
| A08 真增量和独立导出 | 已实现；原始/补偿精确回归与真实数据库端到端通过，待部署 | 未变化材料不重算且可复用比较；补偿参照版本复用；PDF 不重跑全量分析 |
| A09 动态空间预算 | 测量估算/并发预留/旧快照腾挪回归通过；待部署 | 实际产物大小估算；并发导出预算；提前拒绝不足空间 |
| A10 派生产物保留策略 | 已实现并生成 225 真实仓库只读计划；未删除任何数据 | 只读清理计划、引用保护、唯一 blob 大小、指定回退点；实际删除另按计划执行 |
| A11 可复现发布基线 | 固定官方基础、传递依赖锁定及干净预构建通过；待正式提交构建 | 单一固定基础镜像构建；完整源清单/commit/测试/digest；README 和 SECURITY 对齐 |
| A12 行为回归 | 568 项及本地独立浏览器主要流程通过；待线上验收 | 浏览器完整读取/选线/下载及慢网/错误/中断；现有测试继续通过 |
| A13 后端职责拆分 | 本地职责拆分和完整回归通过；待部署验收 | 材料/采集/分析/产物/查询/导出分离，接口与科学 golden 结果一致 |
| A14 共享前端与统计缓存 | 共用请求/路由/控件，统计缓存与测量通过；待部署 | 请求超时/取消/可见性；共用控件与路由；哈希+算法版本缓存；前后测量 |
| A15 桌面视觉统一 | 七视图两种桌面尺寸截图已核对；待上线核对 | 七个视图的大屏/笔记本截图；全名、空态、错误计数、只读布局、图表操作统一 |

## 执行原则

- 科学校验、原始字节、接续顺序、名称与来源追溯继续保留。
- 条件缺陷的复现不代表已存在实验结果错误。当前参数不得自动视为经过人工确认。
- 不以代码行数下降代替职责清晰；不以少数单元测试代替完整流程验收。
- 全部要求完成并验证前保持目标 active；部分实现仅记进展。

## 本轮进展

- 已重新读取报告和当前工作树；开始可靠性修复包。
- 200,400 点样例在 100 点和 6,000 点预览设置下的完整循环及异常统计一致。
- Windows PowerShell 已解析新全量提取脚本，语法通过；未执行仪器数据提取。
- CV/EIS 前后测冲突、模糊候选、未知/已补偿/未补偿、人工确认修订与只读 HTTP 拒绝均有行为测试。
- 本地隔离预览服务仅含合成验证数据，不连接仪器。浏览器操作被用户前台活动打断，尚未完成页面实测；18795/18796 两个测试服务已正常停止，样例目录保留供后续重启。
- 线上仍为 dev.7.10，本轮尚未部署；目标保持 active。
- 当前完整回归：537 项通过（2026-09-07）；Excel/PDF 提取后专项 94 项通过。核心工作区由 6,696 行降至约 5,300 行，尚有任务与图表查询职责待拆分。
- 官方 Docker Hub 元数据已核验 Python 3.12.13-slim-bookworm 多架构摘要：`sha256:4766d8b510c428e595d74b9cc5bbb2fae8e26316fffb4adc89908d79aacd58a2`；linux/amd64 子清单：`sha256:6e13e65c55e33adf203d77ee371cf8bf5d81bd4902ef07565721f46bf44917af`。
- 225 主机 Docker 拉取基础镜像遇到注册表 DNS/直连故障。已用独立临时公开拉取配置避开 SSH 会话的凭据管理器限制；未更改现有 Docker 登录或网络设置，镜像尚未拉取成功。后续可从可联网端校验下载并导入。

## 后续继续位置

1. 完成 A01 真实小样本提取、A02/A04 的浏览器确认流程和 A05 本机/LAN 对照；恢复隔离预览时使用 `diagnostics/audit_preview_server.py`。
2. A08 本地完成：`updated` 在仓库中保留全部材料并只计算缓存缺失/变化项；非仓库独立脚本仍保留原范围选择行为。精确数值 NPY/JSON 缓存与源码/数值库/来源哈希绑定；参照更新会使全部补偿结果失效。可丢弃缓存限制为 512 MiB，不删除原始数据或封存产物。
3. A13 新增独立材料、查询、任务运行、产物发布、任务编排模块，主工作区约 2,100 行。A14 还需共享请求/路由/超时/可见性与实测性能；A15 七个视图还需统一布局和交互。
4. A10 用新只读工具 `scripts/plan_start_stop_retention.py` 在真实仓库生成计划。历史溯源有不可变引用，预览的独占 blob 字节不等于可直接删除空间；不得绕过引用保护。
5. A11 完成可复现发布包、源码提交基线与干净环境构建，最后部署并做完整场景验收。不要以已有专项通过代替全部 A01–A15 完成。

## 增量分析与独立导出验证（2026-09-07）

- 真实 CLI：只改 B，A 不重新解析或计算且仍在完整比较结果中；重命名/编号变化不影响数值缓存。损坏缓存按未命中处理。
- 水位补偿：未变参照不重复拟合；只变普通材料时只重算它；参照源版本变化时全部补偿缓存失效。缓存结果与未缓存科学计算逐项精确一致。
- 真实 SQLite + 正式 worker 子进程：上传→准备材料→原始/补偿分析→封存→独立 PDF 导出通过。导出不物化原始快照；CSV 和补偿模型前后 SHA-256 不变，记录所复用的分析代号。
- 新库首次分析原本可能引用不存在的配置修订 0，导致发布失败；现于首次分析前校验并保存实际默认配置，再封存真实修订号。公开接口保留通用错误，服务端增加异常堆栈诊断。
- 过期、缺少数据模式或旧分析无表结构说明时，PDF 请求返回 409 并提示先分析，不自动重算。材料库按已有分析模式导出。
- 分析来源溯源同时封存依赖模块内容与 SHA-256，任务执行中依赖变化则拒绝发布。
- 拆分后全量回归 550 项通过；尚未部署到 225，浏览器及七个桌面视图验收仍未完成。

## 浏览器与共享请求进展

- 用户已明确允许使用独立 Playwright 测试浏览器（只访问隔离本地服务，不读取日常浏览器资料）。会话 `audit-20260907`，浏览器用户数据目录为自动临时目录。
- `static/start-stop-client.js` 统一七个页面脚本的 JSON 请求和六条导航路由；读取默认 30 s 超时，高亮文件下载 300 s，上传 180 s。取消、响应体卡住、HTTP 冲突、隐藏页暂停/恢复及超时不重试写入有真实 JS 行为测试。
- 原工作站图请求现也能取消旧请求；清空选线时解除 loading 并清除旧图和图例。551 项完整回归通过（CV 布局第一轮后、蓝博工步筛选之前）。
- 合成计算测量：6 个材料、288,000 点、1,200 循环；首次 0.256 s，缓存复用 0.077 s，约 3.3 倍；逐项数值相等。此测量不包含 LAN 下载、原始解析或 PDF 时间。
- 225 仍运行 `0.6.0-dev.7.10-windows`，管理与只读容器健康。本轮没有部署或修改生产数据库。
- 真实仓库保留计划见 `output/platform-audit-implementation/retention-plan-225-20260907.json`：96 代产物，17 代保护、79 代归档候选，候选独占 blob 17,710,577,523 字节（约 16.5 GiB）。不可直接删除，也不承诺立即缩小 SQLite 文件。
- CV–EIS 已在隔离浏览器走通“多个 EIS 候选→人工确认未补偿→过电位可用→确认已在线补偿→禁止重复补偿”；仅修改合成样例。完整源版本绑定由后端测试保障。
- CV–EIS 界面调整：当前 η 在图旁；来源/完整过电位表折叠；确认入口可直接定位表单；未知/已补偿时不勾选离线补偿，也不显示虚假的补偿图例；完整名称换行；SVG 文字按实际像素布局。1440/1920 截图在 `output/playwright/audit-20260907/`，均已打开核对；七页尚未全部验收。
- 新增 `diagnostics/audit_fixture_data.py`，通过真实上传/分析/封存流程生成 22 个工作站材料、6 条蓝博标准化样例，另有 CV/EIS 模糊候选，用于完整桌面回归。所有标为审计样例，不连接仪器。
- 蓝博前后端工步隔离、未知工步单条检查、异常单条、默认近期完整来源已开始实施；缩放/高亮/高亮导出与其余页面视觉仍待完成。
- 原始模式不再将默认 −6.3419 mV/h 当作实际拟合模型；缺少模型时返回 null 并明确显示“本次分析未生成水位补偿模型”。

## 蓝博交互与导出进展

- 工步选择、未知工步单条、异常单条均已有后端拒绝测试；默认从近期完整来源选取同工步记录（最多 16）。不同工步不再被默认合并。
- 新增共用图表交互控制器 `static/start-stop-plot-interaction.js`，供蓝博启停/恒流两个分支使用：框选、平移、滚轮、键盘、右键自动缩放、曲线与图例高亮。几何换算/框选/取消平移/菜单行为有实际 JS 测试。
- 独立浏览器实测：放大至 133%，右键菜单自动缩放回 100%；工步切换 1 条/3 条隔离；高亮按钮、导出按钮状态正常。框选与平移的真实浏览器拖拽还需补验。
- 新增只读 `GET /api/start-stop/stability/chart-export`，原始记录全字段保留，原始表使用文本单元格避免精度丢失/公式执行；处理表使用数值。循环数据用完整统计，全程指标逐条读取原始记录，不从网页抽样点生成。保持单份 BTS 累计时间，不跨不同测试自动接续。
- 父 BTS 版本、路径与 SHA-256 元数据会核对。旧抽样记录、来源改变、空间不足和并发导出会被明确拒绝；使用现有临时空间预算与导出锁。PDF 共用原渲染器，但明确标注 LANBTS 未确认参照，不使用 Hg/HgO/水位补偿标签。
- 已在网页真实点击下载出 Excel 和 PDF。样例 Excel 回读 80 条完整记录、2 个循环，独立计算最后 1 s 中位数一致；五张工作表都已渲染核对。PDF 实际一页、中文与来源说明可读，无缺字或裁切。文件位于 `output/playwright/audit-20260907/.playwright-cli/`，仅为合成审计样例。
- 当前完整回归 **558 项通过**（2026-09-07）；截图和文件验收尚未覆盖全部七个桌面视图，生产环境仍未部署。

## 桌面回归与发布候选（2026-09-07 18:53）

本节为最新状态；前面各节保留历史过程，并非仍待执行的重复任务。

- 完整测试 **568 项通过**；随后新增的“仅改名仍可增量发布”“任务启动错误不被旧成功状态覆盖”专项 6 项通过。发布前还会重新跑完整套件。
- 独立浏览器使用合成库实测上传、准备、材料改名、收藏、保存与刷新。发现并修复：已有配置时新材料被重建工作簿自动选入图集；现在默认不入图，既有选择不变，首次空库与旧独立工作簿流程保持兼容。
- 浏览器实际原始数据增量任务 `b1cfff2a5362`：23 个材料全部复用、0 个重新分析、0 个源文件物化；22 入图、1 不入图，配置 r2 与新名称一致。仅修改名称/入图配置不再被“源文件没更新”门槛拒绝。
- 独立 PDF 任务 `da2e0da26438` 完成，仍为 0 个材料重新计算；在只读入口实际点击并下载原始数据图集。没有强迫普通平台分析生成 PDF。
- 蓝博真实浏览器操作：框选 100%→220%，平移改变坐标范围，右键自动缩放恢复 100%；工步隔离、高亮 Excel/PDF、断网清空旧图并显示错误、恢复后重新读取均通过。将电流请求延迟 2.5 s 后切换全程电压，等待旧请求结束后标题、单位和图仍为全程电压。
- 网络错误改为中文连接提示；任务启动错误保留到成功重试或刷新，不被旧任务的成功消息立即覆盖。JS 行为测试补充这两个分支。
- 七个视图均已在 1440×900 和 1920×1080 下重新截图并打开核对，路径 `output/playwright/audit-20260907/*-final-1440.png` 与 `*-final-1920.png`。全页截图可能高于视口；这是本地合成数据回归，不代表真实实验状态或完整 WCAG 审核。保留原配色、侧栏、圆角与全名；材料只读布局、蓝博异常汇总、CV 前置条件与工作站尾段预览均已核对。
- A01 真实只读验证：144 电脑稳定 BTS 文件提取 601/601 条、stride=1、0 无效行、0 时间倒退，耗时 1119 ms；提取前后原始字节 SHA-256 相同（`1fc36d065e114f00d8460c66c1b8ece482bc80925351331604f501c3eae2008a`）。没有写入生产数据库，没有控制仪器。样本只有一个记录工步，不宣称验证了完整物理启停实验。
- 新增 CSV 行数对账：完整行数＋明确无效行数必须等于提取端声明数量；截断传输不能被误记为完整入库。大样本、循环边界和抽样不影响统计由合成科学回归覆盖。
- A11 基础镜像网络障碍已解决：固定官方 OCI 内容逐项校验后成功导入 225，完整摘要可 inspect；正式 Dockerfile 的预构建成功，未继承旧热修复镜像。依赖直接与传递版本已锁定。该预构建标记 `preflight-uncommitted`，不可当作最终发布。
- 数据库仍为 schema 5，无本轮结构迁移；生产仍为 dev.7.10，未清理备份或历史产物。下一步为提交发布基线、源码清单构建、隔离容器验证、切换和管理端/LAN 对照验收。目标保持 active。

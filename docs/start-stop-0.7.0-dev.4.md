# 0.7.0-dev.4

修复 2026-09-12 代码审查的 8 项问题：HTTP 拒绝请求后的正文残留、连接读取期限与并发上限；实时缺失补偿值；蓝博完整循环判定；CV 最后完整回扫选择；两相独立采样窗口；整数刻度；CV 确认保存后的刷新状态。

- 错误或未消费正文的请求关闭连接；保留合法请求的 HTTP/1.1 复用。每进程最多 32 个连接，请求头/空闲连接期限 15 s、JSON 正文期限 30 s、上传正文期限 300 s，底层 I/O 空闲上限 30 s。
- 缺少水位模型的实时快照保留原始曲线并说明，不生成零电位补偿线。
- 蓝博按连续工步和采样间隔核对完整性；未知尾段不计完整，实时调用方显式传递已闭合证据，单点/时长不足仍拒绝。
- CV 选择最后一个完整阴极回扫；采用此前完整分支时说明排除的尾部记录。两相采样窗口与最终质量检查统一。
- 整数末尾零保留；确认保存成功而读取失败时明确提示刷新失败，不宣称曲线已更新。

数据库 schema 仍为 5，无数据库迁移。原始数据与既有封存结果不自动改写；分析脚本内容参与缓存指纹，新算法首次分析会重新计算。CV 进程内缓存随服务更新重建。

本地 Python 3.11.15 + Node.js 完整回归 620 项全部通过，无跳过。发布必须另外核对已提交归档、镜像摘要、隔离容器、管理端/LAN 配置与分析代号；本文件不代替部署证据。部署沿用原端口、数据卷、SSH、缓存及备份位置，保留原镜像和配置供回退。

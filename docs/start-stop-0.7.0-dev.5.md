# 0.7.0-dev.5

包含 0.7.0-dev.4 的全部审计修复，并修正生产验收中发现的大文件下载超时：响应正文按 64 KiB 分块写出，避免 socket.sendall 的整次调用期限截断仍在持续接收的大 Excel 文件。保留请求读取期限、连接上限和拒绝后关闭连接。

新增真实慢速接收测试：小发送缓冲区、总下载时间超过写超时，核对长度和 SHA-256。关联 40 项测试通过。0.7.0-dev.4 在验收失败后已回退，最终部署使用此版本；不自动改写原始数据和既有封存结果。
